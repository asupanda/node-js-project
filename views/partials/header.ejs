<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>www.sksoles.com</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Atma:wght@300;400;500;600;700&family=Gantari:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['Gantari'] bg-gray-900 text-white pt-16">
    <!-- Navigation Bar -->
    <nav class="w-full fixed top-0 left-0 px-5 py-3 bg-gray-900 z-50 flex justify-between items-center shadow-md">
        <h3 class="text-2xl font-bold">SK Soles</h3>
        
        <% var loggedin = typeof loggedin !== 'undefined' ? loggedin : true; %>
        
        <% if (loggedin) { %>
            <div class="flex gap-x-6">
                <a href="/shop" class="hover:text-gray-300 transition">Shop</a>
                <a href="/cart" class="hover:text-gray-300 transition">Cart</a>
                <a href="#" class="hover:text-gray-300 transition">My Account</a>
                <a href="/users/logout" class="text-red-500 hover:text-red-400 transition">Log Out</a>
            </div>
        <% } %>
    </nav>
    
    